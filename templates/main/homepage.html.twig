<h1>Application <strong>Symfony</strong></h1>

<div>
    Parcourez {{ ships|length * 10 }} vaisseaux spatiaux !

    {% if ships|length > 2 %}
        <p>
            C'est une cargaison de vaisseaux !
        </p>
    {% endif %}
</div>

<div>
    <h2>Mon Vaisseau</h2>

    <table>
        <tr>
            <th>Nom</th>
            <td>{{ myShip.name }}</td>
        </tr>
        <tr>
            <th>Classe</th>
            <td>{{ myShip.class }}</td>
        </tr>
        <tr>
            <th>Capitaine</th>
            <td>{{ myShip.captain|upper|lower|title }}</td>
        </tr>
        <tr>
            <th>Statut</th>
            <td>{{ myShip.status }}</td>
        </tr>
    </table>
</div>
